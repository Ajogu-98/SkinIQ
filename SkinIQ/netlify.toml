[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
```
4. Click **Commit changes**

**Fix 2 — `index.html`**
1. Click on `index.html` → click the **pencil icon**
2. Press **Ctrl+F** to search for:
```
/api/analyze
```
3. Replace it with:
```
/.netlify/functions/analyze
